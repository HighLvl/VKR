table Snapshot {
    name:string;
    x:int64;
    y:int;
}

//Setter Should Begin with Set, suffix and value type should equal one of Snapshot param
table SetX { value:int64;}//value required
table SetY { value:int;}

table SomeAction {
    param1:string; //can be empty
}
table SomeActionResponse {
    value:string; //required
}
